{
  "build": {
    "env": {
      "REDIS_URL": "$REDIS_URL",
      "NEYNAR_API_KEY": "$NEYNAR_API_KEY"
    },
    "use": "@vercel/node"
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/public/$1"
    }
  ]
}
